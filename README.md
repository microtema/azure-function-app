## Azure App Function with Typescript

### Create a local function project
```
func init microtema --typescript
```
```
func new --name json-converter --template "HTTP trigger" --authlevel "anonymous"
```
